export class Keg {
  constructor(public name: string, public brand: string, public price: number, public alcohol_content: number, public ibu: number, public quantity: number, public description: string) {}
}
